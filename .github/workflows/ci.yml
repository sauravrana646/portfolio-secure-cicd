name: ci

on:
  push:
    branches: [dev, uat, main]
  pull_request:
    branches: [dev, uat, main]

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  quality-gate:
    uses: ./.github/workflows/quality-gate.yml
